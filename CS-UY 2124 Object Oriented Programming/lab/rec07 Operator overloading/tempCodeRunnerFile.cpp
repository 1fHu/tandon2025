        // int gcd = greatestCommonDivisor(iniNumerator, iniDenominator);
        // if ((iniDenominator < 0) && (iniNumerator > 0) ){ gcd *= -1; }
        // rhs.numerator = (iniNumerator/gcd);
        // rhs.denominator = (iniDenominator/gcd);