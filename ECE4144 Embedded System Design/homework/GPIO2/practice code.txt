  // // 000
  // delay(2000);

  // // 001
  // PORTD |= (1 << 1); // 1 on
  // delay(1000);
  // PORTD &= ~ (1 << 1); // 1 off
  // delay(1000);

  // // 010
  // PORTD |= (1 << 2); // 2 on
  // delay(1000);
  // PORTD &= ~ (1 << 2); // 2 off
  // delay(1000);

  // // 011
  // PORTD |= (1 << 1); // 1 on
  // PORTD |= (1 << 2); // 2 on
  // delay(1000);
  // PORTD &= ~ (1 << 1); // 1 off
  // PORTD &= ~ (1 << 2); // 2 off
  // delay(1000);

  //   // 100
  // PORTD |= (1 << 3); // 3 on
  // delay(1000);
  // PORTD &= ~ (1 << 3); // 3 off
  // delay(1000);

  // // 101
  // PORTD |= (1 << 1); // 1 on
  // PORTD |= (1 << 3); // 3 on
  // delay(1000);
  // PORTD &= ~ (1 << 1); // 1 off
  // PORTD &= ~ (1 << 3); // 3 off
  // delay(1000);

  // // 110
  // PORTD |= (1 << 2); // 2 on
  // PORTD |= (1 << 3); // 3 on
  // delay(1000);
  // PORTD &= ~ (1 << 2); // 2 off
  // PORTD &= ~ (1 << 3); // 3 off
  // delay(1000);

  // // 111
  // PORTD |= (1 << 1); // 1 on
  // PORTD |= (1 << 2); // 2 on
  // PORTD |= (1 << 3); // 3 on
  // delay(1000);
  // PORTD &= ~ (1 << 1); // 1 off
  // PORTD &= ~ (1 << 2); // 2 off
  // PORTD &= ~ (1 << 3); // 3 off
  // delay(1000);